// Generated by grafana-k6 on 2026-09-05T07:52:37Z
/** Everyone buys the same sku: row-lock contention on inventory reserve, on a sku that cannot run out. */
import { build, scenario } from '../../config/profiles.js';
import { withFixtures } from '../../lib/fixtures/provision.js';
import { catalogFor } from '../../lib/journeys/shopper/data.js';
import { contendSku } from '../../lib/journeys/shopper/contention.js';

export const options = build({ layer: 'shopper', script: 'shopper-inventory-contention', needs: ['store', 'catalog'],
  scenarios: { contention: scenario('rate', 'contention', { rate: 120, unit: '1m' }) },
  thresholds: { 'http_req_duration{name:checkout:checkout}': ['p(95)<3000'] } });
export const { setup, teardown } = withFixtures(options);
export { handleSummary } from '../../lib/core/metrics.js';

export function contention(data) {
  const f = data.fixtures;
  contendSku(f.store, catalogFor(f.store, f), f.products[0].sku);
}
