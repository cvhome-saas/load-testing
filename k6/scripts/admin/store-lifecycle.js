// Generated by grafana-k6 on 2026-09-05T07:52:37Z
/** Onboarding at a low rate: signup → sign-in → create store → provisioned → update → suspend → resume → archive → delete. */
import { build, scenario } from '../../config/profiles.js';
import { withFixtures } from '../../lib/fixtures/provision.js';
import { onboardOrgAndStore } from '../../lib/journeys/admin/storeLifecycle.js';

export const options = build({ layer: 'admin', script: 'admin-store-lifecycle', needs: ['sessions'],
  scenarios: { onboarding: scenario('rate', 'onboarding', { rate: 4, unit: '1m' }) },
  thresholds: { 'journey_duration_ms{journey:store-lifecycle}': ['p(95)<90000'], stores_created: ['count>0'] } });
export const { setup, teardown } = withFixtures(options);
export { handleSummary } from '../../lib/core/metrics.js';

export function onboarding(data) {
  const operator = data.fixtures.sessions.find((s) => s.role === 'SUPER_ADMIN') || null;
  onboardOrgAndStore({ operator });
}
