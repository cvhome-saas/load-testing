// Generated by grafana-k6 on 2026-09-05T07:52:37Z
/** Merchants editing the catalogue of the fixture store: categories, products, price and stock. */
import { build, scenario } from '../../config/profiles.js';
import { withFixtures } from '../../lib/fixtures/provision.js';
import { catalogEdit } from '../../lib/journeys/admin/catalogManagement.js';

export const options = build({ layer: 'admin', script: 'admin-catalog-management', needs: ['store', 'catalog'],
  scenarios: { edits: scenario('rate', 'edits', { rate: 20, unit: '1m' }) } });
export const { setup, teardown } = withFixtures(options);
export { handleSummary } from '../../lib/core/metrics.js';

export function edits(data) {
  const f = data.fixtures;
  catalogEdit(f.store.session, f.store, { categoryId: f.categories.length > 0 ? f.categories[0].id : null });
}
