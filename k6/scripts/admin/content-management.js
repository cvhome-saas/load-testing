// Generated by grafana-k6 on 2026-09-05T07:52:37Z
/** Several editors on one store's content: pages, and the HOME layout's optimistic versioning under concurrency. */
import { build, scenario } from '../../config/profiles.js';
import { withFixtures } from '../../lib/fixtures/provision.js';
import { contentEdit } from '../../lib/journeys/admin/contentManagement.js';

export const options = build({ layer: 'admin', script: 'admin-content-management', needs: ['store'],
  scenarios: { editors: scenario('rate', 'editors', { rate: 12, unit: '1m' }) } });
export const { setup, teardown } = withFixtures(options);
export { handleSummary } from '../../lib/core/metrics.js';

export function editors(data) {
  contentEdit(data.fixtures.store.session, data.fixtures.store);
}
