// Generated by grafana-k6 on 2026-09-05T07:52:37Z
/** Many distinct storefront hosts: spg's domain cache and merchant's lookup-by-domain behind it. */
import execution from 'k6/execution';
import { build, scenario } from '../../config/profiles.js';
import { withFixtures } from '../../lib/fixtures/provision.js';
import { domainLookup, hostsUnderTest } from '../../lib/journeys/platform/spgDomainLookup.js';

export const options = build({ layer: 'platform', script: 'platform-spg-domain-lookup', needs: ['store'], scenarios: { hosts: scenario('rate', 'hosts', { rate: 60, unit: '1m' }) } });
export const { setup, teardown } = withFixtures(options);
export { handleSummary } from '../../lib/core/metrics.js';

export function hosts(data) {
  const list = hostsUnderTest(data.fixtures.store, 50);
  domainLookup(list[execution.scenario.iterationInTest % list.length]);
}
